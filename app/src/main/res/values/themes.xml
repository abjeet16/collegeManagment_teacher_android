<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AttendenceAppTeacher" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Popup Menu Style -->
        <item name="popupMenuStyle">@style/PopupMenuStyle</item>
        <item name="android:itemTextAppearance">@style/MenuTextStyle</item>
    </style>

    <style name="Theme.AttendenceAppTeacher.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AttendenceAppTeacher.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AttendenceAppTeacher.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Custom Menu Text Style -->
    <style name="MenuTextStyle" parent="TextAppearance.AppCompat.Menu">
        <item name="android:textColor">@color/white</item> <!-- Ensures menu text is visible -->
    </style>

    <!-- Custom Popup Menu Style -->
    <style name="PopupMenuStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:popupBackground">@color/black</item> <!-- Ensures drop-down background is dark -->
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.AttendenceAppTeacher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>
